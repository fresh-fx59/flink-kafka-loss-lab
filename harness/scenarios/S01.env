DESCRIPTION="committed-earliest, no checkpoints, auto-commit at its default (false) -> nothing is ever committed, so every restart replays the whole retained log"
STARTING_OFFSETS=committed-earliest
ENABLE_AUTO_COMMIT=false
CHECKPOINTING_MS=0
EXPECT=no-loss
