DESCRIPTION="Tier 2: committed-earliest + explicit auto-commit every 5s -> resume near where the source stopped, outage re-read"
STARTING_OFFSETS=committed-earliest
ENABLE_AUTO_COMMIT=true
AUTO_COMMIT_INTERVAL_MS=5000
CHECKPOINTING_MS=0
EXPECT=no-loss
