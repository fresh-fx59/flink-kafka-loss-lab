DESCRIPTION="checkpoints on, restart WITHOUT -s: are checkpoint-committed offsets enough on their own?"
STARTING_OFFSETS=committed-earliest
ENABLE_AUTO_COMMIT=false
CHECKPOINTING_MS=10000
RESTART_FROM_CHECKPOINT=false
EXPECT=no-loss
