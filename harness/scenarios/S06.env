DESCRIPTION="Tier 1 (the recommendation): checkpoints on, restart WITH -s from the retained checkpoint"
STARTING_OFFSETS=committed-earliest
ENABLE_AUTO_COMMIT=false
CHECKPOINTING_MS=10000
RESTART_FROM_CHECKPOINT=true
EXPECT=no-loss
