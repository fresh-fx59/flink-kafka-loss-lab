DESCRIPTION="the one-line fix for S08: the same injected failure, but it FAILS THE TASK - Flink replays and the rows land"
STARTING_OFFSETS=committed-earliest
ENABLE_AUTO_COMMIT=false
CHECKPOINTING_MS=10000
SINK_FAILURE_MODE=fail-task
SINK_FAIL_SECONDS=25
EXPECT=no-loss
