DESCRIPTION="acknowledge before commit (the Postgres analogue of async_insert without wait-for-ack) -> silent loss with no error anywhere"
STARTING_OFFSETS=committed-earliest
ENABLE_AUTO_COMMIT=true
CHECKPOINTING_MS=0
SINK_FAILURE_MODE=ack-before-commit
SINK_FAIL_SECONDS=45
EXPECT=loss
