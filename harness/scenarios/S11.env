DESCRIPTION="bounded queue that drops instead of back-pressuring -> loss while the job reports healthy"
STARTING_OFFSETS=committed-earliest
ENABLE_AUTO_COMMIT=true
CHECKPOINTING_MS=0
SINK_FAILURE_MODE=drop-on-full
SINK_FAIL_SECONDS=45
EXPECT=loss
