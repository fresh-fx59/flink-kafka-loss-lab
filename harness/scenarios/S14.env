DESCRIPTION="idempotency baseline: replay with plain INSERT -> duplicate rows land"
STARTING_OFFSETS=earliest
ENABLE_AUTO_COMMIT=false
CHECKPOINTING_MS=0
SINK_MODE=plain-insert
PK_ON_EVENT_ID=false
EXPECT=no-loss
